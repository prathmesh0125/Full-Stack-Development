// split
// The split method in JavaScript is used to split a string into an array of substrings based on a specified delimiter.
function splitString(str,delimiter){
  console.log("orignal string: ",str)
  console.log("after split:",str.split(delimiter));
}
let str="i am the ghost of the uchiha"
splitString(str," ");
// delimeter as space and it return a array
